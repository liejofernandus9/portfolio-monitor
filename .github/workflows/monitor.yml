name: Portfolio Monitor

on:
  schedule:
    # 9:45am ET weekdays (1:45pm UTC — EDT = UTC-4)
    - cron: "45 13 * * 1-5"
  workflow_dispatch:

# Allow the workflow to write back to the repo (for dashboard_data.json)
permissions:
  contents: write

jobs:
  run-monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4.2.2
        with:
          # Use a token with write access so git push works
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5.6.0
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Restore trade cache
        uses: actions/cache@v4.2.3
        with:
          path: seen_trades.json
          key: seen-trades-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            seen-trades-${{ runner.os }}-

      - name: Run monitor
        env:
          ALPACA_API_KEY:     ${{ secrets.ALPACA_API_KEY }}
          ALPACA_SECRET_KEY:  ${{ secrets.ALPACA_SECRET_KEY }}
          GEMINI_API_KEY:     ${{ secrets.GEMINI_API_KEY }}
          FINNHUB_API_KEY:    ${{ secrets.FINNHUB_API_KEY }}
          GITHUB_TOKEN:       ${{ secrets.GITHUB_TOKEN }}
        run: python monitor.py

      - name: Save trade cache
        uses: actions/cache@v4.2.3
        with:
          path: seen_trades.json
          key: seen-trades-${{ runner.os }}-${{ github.run_id }}
